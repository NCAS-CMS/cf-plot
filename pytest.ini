[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra -m "not xfail"
markers =
    integration: tests requiring external or reference datasets
    image: tests that compare rendered figures